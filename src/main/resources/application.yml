spring:
  application:
    name: CLI-Toolbox
  config:
    import: optional:file:./config/application-local.yml
  main:
    banner-mode: "off"
    log-startup-info: false
    web-application-type: none
  lifecycle:
    timeout-per-shutdown-phase: 15s
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://127.0.0.1:3306/ykd_bot?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${MYSQL_USERNAME:ykd_app}
    password: ${MYSQL_PASSWORD:}

logging:
  file:
    name: work/app.log
  level:
    root: INFO

app:
  weather:
    api-key: ${WEATHER_API_KEY:}
  intent-ai:
    api-key: ${ARK_API_KEY:}
    base-url: ${ARK_BASE_URL:https://ark.cn-beijing.volces.com/api/v3}
    model: ${ARK_INTENT_MODEL:doubao-seed-2-0-mini-260428}
    max-tokens: ${ARK_INTENT_MAX_TOKENS:350}
    timeout-seconds: ${ARK_INTENT_TIMEOUT_SECONDS:10}
    confidence-threshold: ${INTENT_CONFIDENCE_THRESHOLD:0.70}
  deepseek:
    api-key: ${DEEPSEEK_API_KEY:}
    base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com}
    model: ${DEEPSEEK_MODEL:deepseek-v4-flash}
    max-tokens: ${DEEPSEEK_MAX_TOKENS:1000}
    timeout-seconds: ${DEEPSEEK_TIMEOUT_SECONDS:60}
    thinking-enabled: ${DEEPSEEK_THINKING_ENABLED:false}
    history-rounds: ${DEEPSEEK_HISTORY_ROUNDS:10}
  bailian:
    api-key: ${DASHSCOPE_API_KEY:}
    workspace-id: ${BAILIAN_WORKSPACE_ID:}
    region: ${BAILIAN_REGION:cn-beijing}
    vision-model: ${BAILIAN_VISION_MODEL:qwen3.7-plus}
    image-model: ${BAILIAN_IMAGE_MODEL:qwen-image-2.0}
    image-size: ${BAILIAN_IMAGE_SIZE:1024*1024}
    timeout-seconds: ${BAILIAN_TIMEOUT_SECONDS:180}
    tts:
      model: ${BAILIAN_TTS_MODEL:qwen-audio-3.0-tts-flash}
      voice: ${BAILIAN_TTS_VOICE:longanhuan_v3.6}
      format: ${BAILIAN_TTS_FORMAT:mp3}
      sample-rate: ${BAILIAN_TTS_SAMPLE_RATE:24000}
      timeout-seconds: ${BAILIAN_TTS_TIMEOUT_SECONDS:180}
