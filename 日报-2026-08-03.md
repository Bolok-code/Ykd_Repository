# 日报 — 2026-08-03

## 一、主线合并

将远程 `main` 分支合并到 `dev-czh-skill`，解决 3 个文件冲突：

- `.gitignore` — 保留 `work/` 宽泛规则
- `PlaywrightLiepinAutomationGateway.java` — 取 main 版本（组员的投递链路修复）
- `LiepinJobTaskManager.java` — 取 main 版本（聊一聊+发简历）

合并进来的内容：猎聘新版 IM-UI 适配、简历发送校验、重试机制、Skill 会话管理、多用户登录、退出 Skill 指令。

## 二、RAG 知识库全面修复

### 问题发现
通过日志 + 代码审查，发现 RAG 链路存在 8 个问题：
1. Embedding 批量大小 25 超过 API 上限 10，入库直接报错
2. 检索全量加载 chunk 到内存暴力算相似度（无 LIMIT、无索引）
3. 相似度阈值 0.0 不过滤噪声
4. Skill 路由阈值 0.45 多次差一点未命中
5. 知识库工具是全局工具，无权限控制
6. 查文档名为 N+1 查询
7. 手写 TextChunkService 存在边界 bug + 按字符数切分不如按 token 数
8. 自动入库去重只比文件名

### 修复方案与执行

**P0 小改（commit b53b86e）：**
- EmbeddingService fallback batch size 提常量 DEFAULT_EMBED_BATCH_SIZE=10，触发时打 WARN
- similarityThreshold 默认 0.0→0.3（RagProperties + application.yml 双改）
- RagRetrievalService 检索日志改为 info 级别打印 Top-5 分数分布
- KnowledgeDocumentMapper 新增 findByIds 批量查询，消除 N+1

**P1 架构改动（commit e3b8192）：**
- 新建 `skills/knowledge-base/SKILL.md`，工具白名单 5 个
- SkillToolResolver 注入 KnowledgeBaseTools，注册其 @Tool 方法
- LlmServiceImpl.defaultTools() 移除 knowledgeBaseTools（仅通过 Skill 暴露）
- isExitCommand 正则新增"知识库"
- 更新 EmbeddingSkillSelectorTest 支持三向量区分

**P2 收尾（commit c7ac383）：**
- KnowledgeBaseAutoIngestionService 预检查改为 SHA-256 哈希去重
- 新增 TokenTextSplitterBoundaryTest 验证上限/截断/短文本行为

### 重构：手写 → Spring AI 官方 API
- 删除手写 165 行 `TextChunkService`
- 迁移到 Spring AI 2.0.0 内置 `TokenTextSplitter`（按 token 数切分 + 边界检测）
- chunkSize 从字符维度改为 token 维度（600 字符→300 token）
- 新增 minChunkSizeChars、maxNumChunks 配置项

## 三、文件处理修复

用户发文件时不再强制自动总结。非简历文件收到后，系统改为先告知已收到文件，询问用户意图（存知识库/总结/其他）。

## 四、全项目审计

对剩余代码进行全面扫描，发现 28 个待优化项：

**P0（5 项）：** DocumentTools 文件缓存内存泄漏、LlmServiceImpl NPE 风险、application.yml 假 API key、ThreadLocal 跨线程风险、8 个核心类零测试覆盖

**P1（10 项）：** 知识库表缺复合索引、自动入库损坏文件无限重试、待扫码会话永不超时、知识库工具死参数、无 API 重试配置、Embedding 缓存启动失败不重试、图片批处理无超时、异常恢复时对话不一致、轮询每次写 Session 文件

**P2（12 项）：** 硬编码 TTL、关键词匹配过于激进、int 溢出、无限重试无上限、Thread.sleep 限速、SQLite 相对路径、无对话清理、代码重复等

## 五、提交记录

| Commit | 内容 |
|--------|------|
| be4404b | Merge branch 'main' into dev-czh-skill |
| 3482e7e | fix: Embedding batch size 25→10 |
| 0d7de8d | refactor: TextChunkService → TokenTextSplitter |
| b53b86e | fix(P0): batch size 常量+WARN、threshold 0.3、Top-5 日志、N+1 |
| e3b8192 | feat(P1): knowledge-base Skill + 工具白名单 |
| c7ac383 | fix(P2): 自动入库哈希去重 + TokenTextSplitter 边界验证 |
| 1de3767 | fix: 文件不再自动总结 |

当前 `dev-czh-skill` 分支领先 origin 7 个 commit，等待网络恢复后推送。
